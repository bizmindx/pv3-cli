[project]
name = "django-app"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "django>=5.1",
    "djangorestframework>=3.15",
]

[project.scripts]
serve = "manage:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
